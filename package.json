{
  "name": "transaction-firewall",
  "version": "0.1.0",
  "private": true,
  "description": "Transaction Firewall — a pre-signing security check + co-signer for AI agent EVM transactions",
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "start": "tsx src/server.ts",
    "seed": "tsx scripts/seed.ts",
    "proof": "tsx scripts/realworld-proof.ts",
    "replay": "tsx scripts/historical-replay.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.21.2",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
